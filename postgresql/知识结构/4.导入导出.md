## 将txt/csv文件导入远程数据表中
```
-- 连接数据库
psql -h your_server_ip -p your_port_number -U your_username -d your_database
-- 导入本地文件
\copy users (col1, col2) FROM '$root_path/users.txt' WITH (FORMAT text, DELIMITER ',', HEADER false);
```
